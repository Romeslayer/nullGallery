name: Cypress tests

on: [push]

jobs:
  cypress-run:
    runs-on: unbuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Cypress run
        uses: cypress-io/github-actions@v2
        with:
            build: npm run build
            start: npm start
